---
title: Guild Wars 2 App
---

<div id="app"></div>

<script>
  $(function() {
    console.log(Ember.keys(App.Router.router.recognizer.names));

    App.WorldName.find({}).then(function(){
      App.EventName.find({}).then(function(){
        App.MapName.find({}).then(function(){
          App.advanceReadiness();
        });
      });
    });

    // var promises = [
    //   App.WorldName.find(),
    //   App.EventName.find(),
    //   App.MapName.find()
    // ];

    // $.when.apply(null, promises).done(function() {
    //   console.log('App.advanceReadiness()')
    //   App.advanceReadiness();
    // }).fail(function(){
    //   alert('HOUSTON, THERE WAS A PROBLEM!')
    // });
  });
</script>
